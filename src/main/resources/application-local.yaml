spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_URL}/port_in_scan
    username: ${DB_ID}
    password: ${DB_PW}
  config:
    activate:
      on-profile: local
    import: jwt.yaml